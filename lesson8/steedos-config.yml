datasources:
  default:
    connection: 
      host: 127.0.0.1
      database: steedos
    objectFiles: ["./src/**"]
public:
  cfs:
    store: "local"
    local: 
      folder: "./storage"
cron:
  instancerecordqueue_interval: 10000
plugins:
  - "@steedos/accounts"